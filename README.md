# Todo List app

A simple react app that stores todo list items

## Features
* Add todo items with description and status
* Edit and delete items
* Filter by status and title
* Localstorage

## Running 
First install the dependencies by using `npm install` and then run the server using `npm start`.<br>
You can also build the project with `npm run build` and preview the build with `npm run preview`.